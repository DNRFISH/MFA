% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FISH_query.R
\name{FISH_query}
\alias{FISH_query}
\title{FISH_query}
\usage{
FISH_query(
  con,
  QueryType = "Survey",
  MDNRID = NULL,
  WaterBodyName = NULL,
  SurveyId = NULL,
  SurveyPurpose = NULL,
  Year = NULL,
  GearType = NULL,
  Species = NULL
)
}
\arguments{
\item{con}{A DBI connection to the FISHub database. The connection must
be active when function is called.}

\item{QueryType}{Character string specifying the type of data to return.
Must be one of \code{"Survey"}, \code{"Efforts"}, or \code{"Catch"}. Defaults to
\code{"Survey"}.}

\item{MDNRID}{Character or numeric vector specifying one or more MDNRIDs
to include in the query. If \code{NULL}, data for all waterbodies are
included, subject to the other filters.}

\item{WaterBodyName}{Character vector specifying one or more waterbody
names to include in the query. Names must match the corresponding
\code{WaterBodyName} values in FISHub. If \code{NULL}, all waterbodies are
included, subject to the other filters.}

\item{SurveyId}{Numeric vector specifying one or more Survey IDs to
include in the query. If \code{NULL}, all surveys matching the other
filters are included.}

\item{SurveyPurpose}{Character vector specifying one or more survey
purposes to include in the query. Values must match the survey purpose
descriptions in FISHub. Available survey purposes include
\code{"Creel Census"}, \code{"Discretionary Survey"}, \code{"Fish Community"},
\code{"General Survey"}, \code{"Limnology"}, \code{"Management Evaluation"},
\code{"Population Estimate"}, \code{"Population Reduction"},
\code{"Recruitment Evaluation"}, \code{"Recruitment v Evaluation"},
\code{"Research Project"}, \code{"Special Study"}, \code{"Species Evaluation"},
\code{"Status & Trends"}, and \code{"Stocking Evaluation"}. If \code{NULL}, surveys
of all purposes are included.}

\item{Year}{Numeric vector specifying one or more years to include in
the query. The year is determined from \code{SurveyBeginTimestamp}. If
\code{NULL}, surveys from all years are included, subject to the other
filters.}

\item{GearType}{Character vector specifying one or more gear types to
include in the query. If \code{NULL}, efforts using all gear types are
included. When specified, only surveys and efforts associated with the
selected gear types are returned.}

\item{Species}{Character vector specifying one or more species names to
include in the query. Species names must match the corresponding
species values in FISHub. If \code{NULL}, records for all species are
included. When specified, the query returns only surveys and efforts
associated with catch records for the selected species; efforts with
no recorded catch of the selected species will therefore be excluded.}
}
\value{
A data frame containing the requested FISHub data. The structure of the
returned data depends on \code{QueryType}:

\itemize{
\item \code{"Survey"}: One row per unique survey, including waterbody,
survey purpose and status, survey dates, and survey-level attributes.
\item \code{"Efforts"}: Survey- and effort-level records, including survey
information, gear type, effort dates, number of gears used, and
effort quantities and measurements.
\item \code{"Catch"}: Combined survey-, effort-, and catch-level data,
including species and catch information.
}

The returned data frame contains only records matching the supplied
filter arguments. If no records match the filters, an empty data frame
is returned.
}
\description{
Reads in data from FISHub dynamically to reduce memory burden. Can specify whether to query survey or efforts.
Filters allow you to filter by waterbody, surveyID, survey purpose, year, or gear type
}
\details{
\code{FISH_query()} dynamically queries FISHub using the supplied filters and
collects only the resulting records into R. This approach is intended to
reduce memory use compared with retrieving complete database tables.

The query combines information from the \code{WaterBody}, \code{Survey},
\code{SurveyPurpose}, \code{SurveyStatus}, \code{SurveyEffort}, \code{SurveyEffortDetail},
and \code{Gear} tables. Catch information is subsequently added using
\code{\link[=catchByEffort]{catchByEffort()}}.

All filter arguments are optional. When multiple filter values are
supplied, records matching any of the specified values are retained
within each filter, while different filters are applied simultaneously.
For example, specifying multiple \code{SurveyId} values returns data for any
of those surveys, while also applying any specified \code{Year} or \code{GearType}
filters.

The \code{QueryType} argument determines the level of data returned. \code{"Survey"}
returns one row per unique survey, \code{"Efforts"} returns survey effort-level
records, and \code{"Catch"} returns the combined survey, effort, and catch data.

When \code{GearType} is specified, only surveys and efforts associated with the
specified gear types are retained. When \code{Species} is specified, only
surveys and efforts with recorded catch of the specified species are
retained. Consequently, specifying \code{Species} excludes efforts in which
the selected species was not caught or was not recorded. This should be
considered when using the returned data to calculate catch rates or CPUE.

If no records match the specified filters, an empty data frame is returned
and a message is printed indicating that no data were found.
}
\examples{
\dontrun{
#connect to database
con <- FISHub_connect()

#single survey; note different query types
FISH_Data <- FISH_query(con,QueryType = "Survey",SurveyId = 805)
FISH_Data <- FISH_query(con,QueryType = "Efforts",SurveyId = 805)
FISH_Data <- FISH_query(con,QueryType = "Catch",SurveyId = 805)

#all surveys from a waterbody
FISH_Data <- FISH_query(con,QueryType = "Survey",WaterBodyName = "Lake Orion")

#all SnT surveys from 2025
FISH_Data <- FISH_query(con,QueryType = "Survey",SurveyPurpose = "Status & Trends",Year=2025)

#all surveys that caught bowfin in 2025
FISH_Data <- FISH_query(con,QueryType = "Survey",Species="Bowfin",Year=2025)

#disconnect from database
DBI::dbDisconnect(con)
}

}
